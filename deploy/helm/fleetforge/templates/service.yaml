apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleetforge.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fleetforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fleetforge.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
